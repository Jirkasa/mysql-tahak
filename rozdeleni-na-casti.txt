V tomto souboru si rozepíšu části webových stránek:

DATABÁZE A TABULKY, DATOVÉ TYPY, CRUD OPERACE, PRIMARY A FOREIGN KEY, STRING FUNKCE, DATUMOVÉ FUNKCE, LOGICKÉ OPERÁTORY, VZTAHY MEZI TABULKAMI, TRIGGERY
- zatím to teda vypadá takto, možná to později ještě změním

- neslouží to jako tutoriál - nemá to učit - má to připomínat

zbývá: STRING FUNKCE, DATUMOVOVÉ FUNKCE


DATABÁZE A TABULKY - ZATÍM HOTOVO
    SHOW DATABASES - HOTOVO
    CREATE DATABASE - HOTOVO
    DROP DATABASE - HOTOVO
    CREATE TABLE - HOTOVO
        NOT NULL - HOTOVO
        DEFAULT - HOTOVO
        UNIQUE - HOTOVO
    DROP TABLE - HOTOVO
    SELECT database(); - HOTOVO
    USE database; - HOTOVO
    SHOW TABLES; - HOTOVO
    SHOW COLUMNS FROM table; - HOTOVO
    DESC table; - HOTOVO
        DESCRIBE - HOTOVO

DATOVÉ TYPY - HOTOVO

DATUMOVÉ FUNKCE
    CURDATE
    CURTIME
    DAY, DAYNAME, DAYOFYEAR, DATE_FORMAT
    DATEDIFF
    DATE_ADD, DATE_SUB

CRUD OPERACE - ZATÍM HOTOVO
    INSERT - HOTOVO
        INSERT INTO table ... - HOTOVO
        - i ta druhá verze pokud je - HOTOVO
    SELECT
        AS - HOTOVO
        DISTINCT - HOTOVO
        ORDER BY - HOTOVO
            ASC, DESC - HOTOVO
        LIMIT - HOTOVO
        LIKE - toto napíšu do LOGICKÉ OPERÁTORY - HOTOVO
        COUNT - HOTOVO
        SUM - HOTOVO
        AVG - HOTOVO
        ROUND - HOTOVO
        IF funkce - možná až do LOGICKÉ OPERÁTORY - HOTOVO
        MIN MAX - HOTOVO
        IFNULL - HOTOVO
        GROUP BY - HOTOVO
            HAVING - HOTOVO
        WHERE - tady ty věci se dají použít i u jiných operací, nebudu to dávat přímo k select ale až někam dole možná - HOTOVO
    UPDATE - HOTOVO
    DELETE - HOTOVO
    - subquery - HOTOVO
    SHOW WARNING; - HOTOVO
    spouštění kódu ze souboru: source soubor.sql; - to dám asi do DATABÁZE A TABULKY - ale taky to tam úplně nepatří, tak to tam musím napsat že to tam úplně nepatří - HOTOVO

PRIMARY A FOREIGN KEY - ZATÍM HOTOVO
        AUTO_INCREMENT - HOTOVO
        NOT NULL - HOTOVO
        ON DELETE CASCADE - HOTOVO
        primary key jako více hodnot - HOTOVO

STRING FUNCTIONS - ZATÍM HOTOVO
    - asi jen nějaké, všechny ne, na ostatní přidám odkaz
    CONCAT - HOTOVO
    CONCAT_WS - HOTOVO
    SUBSTRING - HOTOVO
    REPLACE - HOTOVO
    CHAR_LENGTH - HOTOVO
    UPPER - HOTOVO
    LOWER - HOTOVO

LOGICKÉ OPERÁTORY - ZATÍM HOTOVO
    - operátory - HOTOVO
    CAST - HOTOVO
    BETWEEN - HOTOVO
    NOT BETWEEN - HOTOVO
    LIKE - HOTOVO
    NOT LIKE - HOTOVO
    IN - HOTOVO
    NOT IN - HOTOVO
    CASE - HOTOVO
        WHEN THEN ... - HOTOVO
    IS NULL - HOTOVO

    https://dev.mysql.com/doc/refman/8.0/en/comparison-operators.html
    https://dev.mysql.com/doc/refman/8.0/en/logical-operators.html

VZTAHY MEZI TABULKAMI - ZATÍM HOTOVO
    ONE TO ONE - HOTOVO
    ONE TO MANY - HOTOVO
    MANY TO MANY - HOTOVO
    - FOREIGN KEY - TO UŽ MÁM
    FROM z více tabulek (CROSS JOIN) - HOTOVO
    INNER JOIN, LEFT JOIN, RIGHT JOIN - HOTOVO
    více JOINŮ za sebou - HOTOVO

TRIGGERY - HOTOVO




nástroj pro highlightování kódu: https://highlight.hohli.com/